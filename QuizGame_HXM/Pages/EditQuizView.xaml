<UserControl x:Class="QuizGame_HXM.Pages.EditQuizView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <!-- Left: Question List -->
        <ListBox x:Name="QuestionList"
                 DisplayMemberPath="QuestionText"
                 SelectionChanged="QuestionList_SelectionChanged"
                 Margin="0,0,10,0" />

        <!-- Right: Editor Panel -->
        <StackPanel Grid.Column="1" Margin="10,0,0,0">
            <TextBlock Text="Question Text" FontWeight="Bold" />
            <TextBox x:Name="QuestionTextBox" Width="400" Margin="0,0,0,10"/>

            <TextBlock Text="Answer 1" FontWeight="Bold" />
            <TextBox x:Name="Answer1Box" Width="300" Margin="0,0,0,5"/>

            <TextBlock Text="Answer 2" FontWeight="Bold" />
            <TextBox x:Name="Answer2Box" Width="300" Margin="0,0,0,5"/>

            <TextBlock Text="Answer 3" FontWeight="Bold" />
            <TextBox x:Name="Answer3Box" Width="300" Margin="0,0,0,5"/>

            <TextBlock Text="Correct Answer Index (0-2)" FontWeight="Bold" />
            <TextBox x:Name="CorrectIndexBox" Width="50" Margin="0,0,0,5"/>

            <TextBlock Text="Category" FontWeight="Bold" />
            <TextBox x:Name="CategoryBox" Width="300" Margin="0,0,0,15"/>

            <StackPanel Orientation="Horizontal">
                <Button Content="Apply Changes" Click="ApplyChanges_Click" Width="120" Margin="0,0,10,0"/>
                <Button Content="Save Quiz" Click="SaveQuiz_Click" Width="100" />
                <Button Content="Back to Menu" Click="BackToMenu_Click" Width="120" Margin="10,0,0,0" />
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>